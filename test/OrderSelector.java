/**
 * class OrderSelector
 *
 * Class to select order from a given array of orders
 */
class OrderSelector
{
    public static Order select(CustomerDetails cus, Order[] orders)
    {
        // Business logic to find the best order from the given set of orders
        Order bestOrder;
        return bestOrder;
        // Dummy order, use return orders[i] where i-th order is the optimal order
    }
}
